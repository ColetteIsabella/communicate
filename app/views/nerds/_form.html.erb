<%= form_for(@nerd) do |f| %>
  <% if @nerd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nerd.errors.count, "error") %> prohibited this nerd from being saved:</h2>

      <ul>
      <% @nerd.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nameA, "Question Seeker"%>
    <%= f.text_field :nameA %>
  </div>
  <div class="field">
    <%= f.label :question, "Question" %><br />
    <%= f.text_area :question %>
  </div>

  <%= f.submit "Submit" %>
<% end %>
