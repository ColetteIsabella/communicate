<h1>Ask a question, answer a question</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @nerds.each do |nerd| %>
  <tr>
    <td><%= nerd.nameA %></td>
    <td><%= nerd.question %></td>
    <td><%= nerd.nameB %></td>
    <td><%= nerd.answer %></td>
    <td><%= link_to 'Show', nerd %></td>
    <td><%= link_to 'Edit', edit_nerd_path(nerd) %></td>
    <td><%= link_to 'Destroy', nerd, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Question', new_nerd_path %>
